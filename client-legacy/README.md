# kyponet-client-legacy

Test:

```shell
python3 -m tox
```

Install:

```shell
pip install .
```

Run with a configuration generated by master:

```shell
cat config.json | kyponet-client-legacy setup
```
